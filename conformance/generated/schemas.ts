// Generated by @chanx-js/codegen. Do not edit.
// Regenerate with: npx @chanx-js/codegen

/** Analytics message. */
export interface AnalyticsMessage {
  action: 'analytics';
  payload: AnalyticsPayload;
}

/** Analytics notification message. */
export interface AnalyticsNotificationMessage {
  action: 'analytics_notification';
  payload: AnalyticsPayload;
}

export interface AnalyticsPayload {
  event: string;
  data?: unknown;
}

/** Chat message for the basic chat consumer. */
export interface ChatMessage {
  action: 'chat';
  payload: ChatPayload;
}

/** Chat notification message. */
export interface ChatNotificationMessage {
  action: 'chat_notification';
  payload: ChatPayload;
}

export interface ChatPayload {
  message: string;
}

/** Ask for an acknowledgement without broadcasting. */
export interface EchoMessage {
  action: 'echo';
  payload: PostPayload;
}

/** Event forwarded straight to the client by a mixin's passthrough_events. */
export interface ExtraPassthroughMessage {
  action: 'extra_passthrough';
  payload: string;
}

/** Extra request message for ws handler mixin. */
export interface ExtraRequestMessage {
  action: 'extra_request';
  payload: string;
}

/** Extra response message returned by handler mixins. */
export interface ExtraResponseMessage {
  action: 'extra_response';
  payload: string;
}

/** Background job message. */
export interface JobMessage {
  action: 'job';
  payload: JobPayload;
}

export interface JobPayload {
  type?: string;
  content: string;
}

/** Job status message. */
export interface JobStatusMessage {
  action: 'job_status';
  payload: Record<string, unknown>;
}

export interface MessagePayload {
  message: string;
}

export type MessageStatus = 'PENDING' | 'DELIVERED' | 'FAILED';

/** Notification broadcast message. */
export interface NotificationBroadcastMessage {
  action: 'notification_broadcast';
  payload: NotificationPayload;
}

/** Notification message. */
export interface NotificationMessage {
  action: 'notification';
  payload: NotificationPayload;
}

export interface NotificationPayload {
  type?: string;
  message: string;
}

/** Simple ping message to check WebSocket connection status. */
export interface PingMessage {
  action: 'ping';
  payload?: null;
}

/** Simple pong message response to ping requests. */
export interface PongMessage {
  action: 'pong';
  payload?: null;
}

/** Post something to a room. */
export interface PostMessage {
  action: 'post';
  payload: PostPayload;
}

export interface PostPayload {
  body: string;
  author?: string;
}

/** Confirm a post. */
export interface PostedMessage {
  action: 'posted';
  payload: PostPayload;
}

/** A user's presence changed. */
export interface PresenceChanged {
  action: 'presence_changed';
  payload: PresencePayload;
}

export interface PresencePayload {
  user: string;
  online: boolean;
}

/** Reliable chat message. */
export interface ReliableChatMessage {
  action: 'reliable_chat';
  payload: ReliableChatPayload;
}

/** Reliable chat notification message. */
export interface ReliableChatNotificationMessage {
  action: 'reliable_chat_notification';
  payload: ReliableChatPayload;
}

export interface ReliableChatPayload {
  message: string;
}

/** Room chat message. */
export interface RoomChatMessage {
  action: 'room_chat';
  payload: RoomMessagePayload;
}

/** Something happened in a room. */
export interface RoomEvent {
  action: 'room_event';
  payload: PostPayload;
}

export interface RoomMessagePayload {
  message: string;
  room_name?: string | null;
  status?: MessageStatus;
}

/** Room notification message. */
export interface RoomNotificationMessage {
  action: 'room_notification';
  payload: RoomMessagePayload;
}

/** System echo response message. */
export interface SystemEchoMessage {
  action: 'system_echo';
  payload: MessagePayload;
}

/** Notification when a user joins the chat. */
export interface UserJoinedNotification {
  action: 'user_joined_notification';
  payload: ChatPayload;
}

/** Notification when a user leaves the chat. */
export interface UserLeftNotification {
  action: 'user_left_notification';
  payload: ChatPayload;
}

/** System message for direct communication. */
export interface UserMessage {
  action: 'user_message';
  payload: MessagePayload;
}
