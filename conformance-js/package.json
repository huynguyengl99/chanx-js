{
  "name": "chanx-conformance-js",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "A plain-JavaScript consumer of the generated client, type-checked with checkJs. Proves `--emit js` gives a JS project the same narrowing a TS project gets.",
  "scripts": {
    "gen": "tsx ../packages/codegen/src/cli.ts ../packages/codegen/tests/__fixtures__/fastapi-asyncapi.json -o generated --emit js",
    "typecheck": "tsc -p jsconfig.json",
    "typecheck:dist": "tsc -p jsconfig.dist.json"
  },
  "dependencies": {
    "@chanx-js/client": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.0"
  }
}
